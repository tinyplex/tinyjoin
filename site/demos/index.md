# Demos

These small examples focus on application behavior. TinyGres keeps its Worker,
WebAssembly, and storage implementation behind the ordinary create() call.

The [create-tinygres](https://github.com/tinyplex/create-tinygres) package can
generate a complete Vite starter using the same patterns.
